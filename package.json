{
  "name": "fireworks",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://bezi.io",
  "dependencies": {
    "flow-bin": "^0.76.0",
    "prettier": "^1.13.7",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build -b master"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
